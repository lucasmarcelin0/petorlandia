# Auditoria de Rotas HTTP (multi-tenant)

|Criticidade|Tipo de dado|Escopo|Arquivo:linha|Rota(s)|Decorators|Autorização efetiva|IDs (origem)|Proteção esperada|Proteção atual|Gap|
|---|---|---|---|---|---|---|---|---|---|---|
|alta|clínico|clínica|`app.py:2782`|`app.route('/animal/<int:animal_id>/historico_consultas', methods=['GET'])`|`login_required`|ensure_*, clinic_id check|URL path, query|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|financeiro|usuário|`app.py:3431`|`app.route('/veterinario/assinatura/checkout', methods=['POST'])`|`login_required`|ensure_*, abort(403), is_admin|-|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|financeiro|usuário|`app.py:3540`|`app.route('/veterinario/assinatura/<int:membership_id>/nova_avaliacao', methods=['POST'])`|`login_required`|abort(403), is_admin|URL path|login + validação ownership + escopo clínica|forte|ID na URL sem validação de escopo clara;|
|alta|clínico|clínica|`app.py:3930`|`app.route('/painel')`|`login_required`|abort(403), is_admin|-|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|financeiro|clínica|`app.py:11184`|`app.route('/animals')`|`-`|authorize*, is_admin, clinic_id check, ownership|query|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|clínica|`app.py:12376`|`app.route('/animal/<int:animal_id>/termo/<string:tipo>')`|`login_required`|ownership|URL path|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|clínica|`app.py:12741`|`app.route('/animal/<int:animal_id>/ficha')`|`login_required`|clinic_id check, ownership|URL path, query|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|usuário|`app.py:12940`|`app.route('/animal/<int:animal_id>/editar_ficha', methods=['GET', 'POST'])`|`login_required`|-|URL path, form-data|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|clínico|usuário|`app.py:12961`|`app.route('/generate_qr/<int:animal_id>')`|`login_required`|-|URL path|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|financeiro|clínica|`app.py:12993`|`app.route('/consulta_qr', methods=['GET'])`|`login_required`|clinic_id check, ownership|query|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|clínica|`app.py:13244`|`app.route('/consulta/<int:consulta_id>/sugestoes_clinicas', methods=['POST'])`|`login_required`|ensure_*, clinic_id check|URL path, json body|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|clínica|`app.py:13280`|`app.route('/consulta/<int:consulta_id>/sugestoes_clinicas/feedback', methods=['POST'])`|`login_required`|ensure_*|URL path, json body|login + validação ownership + escopo clínica|forte|ID na URL sem validação de escopo clara;|
|alta|clínico|clínica|`app.py:13311`|`app.route('/consulta/<int:consulta_id>/sugestoes_clinicas/aplicar', methods=['POST'])`|`login_required`|ensure_*, clinic_id check|URL path, json body|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|clínica|`app.py:13419`|`app.route('/consulta/<int:consulta_id>/sugestoes_clinicas/protocolos', methods=['POST'])`|`login_required`|ensure_*|URL path, json body|login + validação ownership + escopo clínica|forte|ID na URL sem validação de escopo clara;|
|alta|financeiro|clínica|`app.py:13462`|`app.route('/consulta/<int:animal_id>')`|`login_required`|ensure_*, abort(403), clinic_id check, ownership|URL path, query|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|clínica|`app.py:13661`|`app.route('/finalizar_consulta/<int:consulta_id>', methods=['POST'])`|`login_required`|clinic_id check|URL path|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|clínica|`app.py:13696`|`app.route('/finalizar_consulta/<int:consulta_id>/fechar', methods=['POST'])`|`login_required`|clinic_id check|URL path|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|clínica|`app.py:13733`|`app.route('/agendar_retorno/<int:consulta_id>', methods=['POST'])`|`login_required`|abort(403), clinic_id check|URL path, form-data|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|clínica|`app.py:13792`|`app.route('/retorno/<int:appointment_id>/start', methods=['POST'])`|`login_required`|abort(403), clinic_id check|URL path|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|usuário|`app.py:13811`|`app.route('/consulta/<int:consulta_id>/deletar', methods=['POST'])`|`login_required`|-|URL path|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|financeiro|clínica|`app.py:13839`|`app.route('/imprimir_consulta/<int:consulta_id>')`|`login_required`|ownership|URL path|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|financeiro|clínica|`app.py:16894`|`app.route('/tutores', methods=['GET', 'POST'])`|`login_required`|clinic_id check|query, form-data|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|usuário|`app.py:17352`|`app.route('/deletar_tutor/<int:tutor_id>', methods=['POST'])`|`login_required`|abort(403)|URL path|login + validação ownership + escopo clínica|forte|ID na URL sem validação de escopo clara;|
|alta|clínico|clínica|`app.py:17765`|`app.route('/update_consulta/<int:consulta_id>', methods=['POST'])`|`login_required`|clinic_id check|URL path, query, form-data|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|financeiro|usuário|`app.py:17865`|`app.route('/animal/<int:animal_id>/racoes', methods=['POST'])`|`login_required`|-|URL path, json body|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|financeiro|usuário|`app.py:18092`|`app.route('/racao/<int:racao_id>', methods=['GET', 'PUT', 'DELETE'])`|`login_required`|-|URL path, json body|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|clínico|usuário|`app.py:19015`|`app.route('/buscar_vacinas')`|`-`|-|query|login + validação ownership + escopo clínica|parcial|Sem login_required explícito;Sem checagem de autorização/ownership evidente;|
|alta|clínico|usuário|`app.py:19053`|`app.route('/vacina_modelo', methods=['POST'])`|`login_required`|-|json body|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;|
|alta|clínico|usuário|`app.py:19098`|`app.route('/vacina_modelo/<int:vacina_id>', methods=['PUT', 'DELETE'])`|`login_required`|-|URL path, json body|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|clínico|usuário|`app.py:19127`|`app.route("/animal/<int:animal_id>/vacinas", methods=["POST"])`|`-`|-|URL path, json body|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|financeiro|clínica|`app.py:19175`|`app.route("/animal/<int:animal_id>/vacinas/imprimir")`|`-`|-|URL path, query|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|clínico|usuário|`app.py:19199`|`app.route('/vacina/<int:vacina_id>', methods=['PUT', 'DELETE'])`|`login_required`|-|URL path, json body|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|clínico|usuário|`app.py:19298`|`app.route('/consulta/<int:consulta_id>/prescricao', methods=['POST'])`|`login_required`|-|URL path, form-data|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|clínico|clínica|`app.py:19346`|`app.route('/prescricao/<int:prescricao_id>/deletar', methods=['POST'])`|`login_required`|ensure_*, clinic_id check|URL path|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|usuário|`app.py:19494`|`app.route("/buscar_medicamentos")`|`-`|-|query|login + validação ownership + escopo clínica|parcial|Sem login_required explícito;Sem checagem de autorização/ownership evidente;|
|alta|clínico|usuário|`app.py:19638`|`app.route("/medicamentos_frequentes")`|`login_required`|-|-|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;|
|alta|clínico|clínica|`app.py:19698`|`app.route('/consulta/<int:consulta_id>/historico_prescricoes', methods=['GET'])`|`login_required`|ensure_*, clinic_id check|URL path|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|clínica|`app.py:19718`|`app.route('/animal/<int:animal_id>/historico_exames', methods=['GET'])`|`login_required`|ensure_*, clinic_id check|URL path, query|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|clínica|`app.py:19735`|`app.route('/animal/<int:animal_id>/historico_prescricoes', methods=['GET'])`|`login_required`|ensure_*, clinic_id check|URL path, query|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|clínica|`app.py:19749`|`app.route('/consulta/<int:consulta_id>/prescricao/lote', methods=['POST'])`|`login_required`|-|URL path, json body|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|clínico|clínica|`app.py:19781`|`app.route('/consulta/<int:consulta_id>/bloco_prescricao', methods=['POST'])`|`login_required`|ensure_*, clinic_id check|URL path, json body|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|clínica|`app.py:19859`|`app.route('/bloco_prescricao/<int:bloco_id>/deletar', methods=['POST'])`|`login_required`|ensure_*, clinic_id check|URL path|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|financeiro|clínica|`app.py:19885`|`app.route('/bloco_prescricao/<int:bloco_id>/editar', methods=['GET'])`|`login_required`|ensure_*|URL path|login + validação ownership + escopo clínica|forte|ID na URL sem validação de escopo clara;|
|alta|clínico|clínica|`app.py:19898`|`app.route('/bloco_prescricao/<int:bloco_id>/atualizar', methods=['POST'])`|`login_required`|ensure_*|URL path, json body|login + validação ownership + escopo clínica|forte|ID na URL sem validação de escopo clara;|
|alta|financeiro|clínica|`app.py:19955`|`app.route('/bloco_prescricao/<int:bloco_id>/imprimir')`|`login_required`|ensure_*, ownership|URL path|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|usuário|`app.py:19996`|`app.route('/animal/<int:animal_id>/bloco_exames', methods=['POST'])`|`login_required`|-|URL path, json body|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|clínico|usuário|`app.py:20028`|`app.route('/buscar_exames')`|`login_required`|-|query|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;|
|alta|clínico|usuário|`app.py:20059`|`app.route('/exame_modelo', methods=['POST'])`|`login_required`|-|json body|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;|
|alta|clínico|usuário|`app.py:20073`|`app.route('/exame_modelo/<int:exame_id>', methods=['PUT', 'DELETE'])`|`login_required`|-|URL path, json body|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|financeiro|clínica|`app.py:20094`|`app.route('/imprimir_bloco_exames/<int:bloco_id>')`|`login_required`|ownership|URL path, query|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|usuário|`app.py:20121`|`app.route('/bloco_exames/<int:bloco_id>/deletar', methods=['POST'])`|`login_required`|-|URL path|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|financeiro|clínica|`app.py:20147`|`app.route('/bloco_exames/<int:bloco_id>/editar', methods=['GET'])`|`login_required`|ensure_*|URL path|login + validação ownership + escopo clínica|forte|ID na URL sem validação de escopo clara;|
|alta|clínico|clínica|`app.py:20160`|`app.route('/exame/<int:exame_id>', methods=['PUT', 'DELETE'])`|`login_required`|ensure_*|URL path, json body|login + validação ownership + escopo clínica|forte|ID na URL sem validação de escopo clara;|
|alta|clínico|clínica|`app.py:20204`|`app.route('/bloco_exames/<int:bloco_id>/atualizar', methods=['POST'])`|`login_required`|ensure_*|URL path, json body|login + validação ownership + escopo clínica|forte|ID na URL sem validação de escopo clara;|
|alta|clínico|clínica|`app.py:20258`|`app.route('/bloco_exames/<int:bloco_id>/realizacao', methods=['POST'])`|`login_required`|ensure_*|URL path, json body|login + validação ownership + escopo clínica|forte|ID na URL sem validação de escopo clara;|
|alta|clínico|usuário|`app.py:20303`|`app.route('/novo_atendimento')`|`login_required`|-|-|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;|
|alta|financeiro|clínica|`app.py:20357`|`app.route('/novo_animal', methods=['GET', 'POST'])`|`login_required`|clinic_id check|query, form-data|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|financeiro|clínica|`app.py:24603`|`app.route("/appointments/<int:appointment_id>/close", methods=["GET", "POST"])`|`login_required`|abort(403), clinic_id check|URL path|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|financeiro|clínica|`app.py:26623`|`app.route('/imprimir_orcamento/<int:consulta_id>')`|`login_required`|ownership|URL path|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|financeiro|clínica|`app.py:26646`|`app.route('/imprimir_bloco_orcamento/<int:bloco_id>')`|`login_required`|ensure_*, ownership|URL path|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|financeiro|clínica|`app.py:26674`|`app.route('/orcamento/<int:orcamento_id>/imprimir')`|`login_required`|-|URL path|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|financeiro|clínica|`app.py:26692`|`app.route('/pagar_orcamento/<int:bloco_id>', methods=['GET', 'POST'])`|`login_required`|ensure_*, ownership|URL path|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|financeiro|clínica|`app.py:26805`|`app.route('/orcamento/<int:orcamento_id>/pagar', methods=['POST'])`|`login_required`|-|URL path|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|financeiro|usuário|`app.py:26866`|`app.route('/consulta/<int:consulta_id>/pagar_orcamento')`|`login_required`|-|URL path|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|financeiro|clínica|`app.py:26914`|`app.route('/consulta/<int:consulta_id>/orcamento_item', methods=['POST'])`|`login_required`|clinic_id check|URL path, json body|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|financeiro|clínica|`app.py:26991`|`app.route('/consulta/orcamento_item/<int:item_id>', methods=['DELETE'])`|`login_required`|-|URL path|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|financeiro|clínica|`app.py:27005`|`app.route('/consulta/<int:consulta_id>/bloco_orcamento', methods=['POST'])`|`login_required`|clinic_id check|URL path, json body|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|financeiro|clínica|`app.py:27073`|`app.route('/consulta/<int:consulta_id>/historico_orcamentos', methods=['GET'])`|`login_required`|clinic_id check|URL path|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|financeiro|clínica|`app.py:27086`|`app.route('/bloco_orcamento/<int:bloco_id>/deletar', methods=['POST'])`|`login_required`|clinic_id check|URL path|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|financeiro|clínica|`app.py:27108`|`app.route('/bloco_orcamento/<int:bloco_id>/editar', methods=['GET'])`|`login_required`|-|URL path|login + validação ownership + escopo clínica|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|alta|financeiro|clínica|`app.py:27119`|`app.route('/bloco_orcamento/<int:bloco_id>/atualizar', methods=['POST'])`|`login_required`|clinic_id check|URL path, json body|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|alta|clínico|clínica|`app.py:27538`|`app.route("/api/animal/<int:animal_id>/peso", methods=["PATCH"])`|`csrf.exempt, login_required`|ownership|URL path, json body|login + validação ownership + escopo clínica|forte|sem gap crítico aparente|
|média|pessoal|usuário|`app.py:3400`|`app.route('/veterinario/assinatura')`|`login_required`|ensure_*, abort(403), is_admin|query|login + autorização objeto|forte|sem gap crítico aparente|
|média|pessoal|usuário|`app.py:3505`|`app.route('/veterinario/assinatura/<int:membership_id>/cancelar_avaliacao', methods=['POST'])`|`login_required`|abort(403), is_admin|URL path|login + autorização objeto|forte|ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:7835`|`app.route('/add-animal', methods=['GET', 'POST'])`|`login_required`|ownership|form-data|login + autorização objeto|forte|sem gap crítico aparente|
|média|pessoal|usuário|`app.py:11179`|`app.route('/meus-animais')`|`login_required`|-|-|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;|
|média|pessoal|usuário|`app.py:11310`|`app.route('/animal/<int:animal_id>/adotar', methods=['POST'])`|`login_required`|-|URL path|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:11327`|`app.route('/animal/<int:animal_id>/editar', methods=['GET', 'POST']) | app.route('/editar_animal/<int:animal_id>', metho`|`login_required`|-|URL path, form-data|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:12030`|`app.route('/animal/<int:animal_id>/deletar', methods=['POST'])`|`login_required`|abort(403)|URL path|login + autorização objeto|forte|ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:12076`|`app.route('/termo/interesse/<int:animal_id>/<int:user_id>', methods=['GET', 'POST'])`|`login_required`|-|URL path|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:12315`|`app.route('/termo/transferencia/<int:animal_id>/<int:user_id>', methods=['GET', 'POST'])`|`login_required`|ownership|URL path|login + autorização objeto|forte|sem gap crítico aparente|
|média|pessoal|usuário|`app.py:12404`|`app.route('/termo/<int:animal_id>/<tipo>')`|`login_required`|ownership|URL path|login + autorização objeto|forte|sem gap crítico aparente|
|média|pessoal|usuário|`app.py:12864`|`app.route('/animal/<int:animal_id>/documentos', methods=['POST'])`|`login_required`|-|URL path, form-data|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:12906`|`app.route('/animal/<int:animal_id>/documentos/<int:doc_id>/delete', methods=['POST'])`|`login_required`|-|URL path|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|média|pessoal|clínica|`app.py:13869`|`app.route('/buscar_tutores', methods=['GET'])`|`login_required`|is_admin, clinic_id check|query|login + autorização objeto|forte|sem gap crítico aparente|
|média|pessoal|usuário|`app.py:16870`|`app.route('/tutor/<int:tutor_id>')`|`login_required`|-|URL path|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:16886`|`app.route('/tutor/<int:tutor_id>')`|`login_required`|-|URL path|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:17118`|`app.route('/tutor/compartilhamentos')`|`login_required`|abort(403)|query|login + autorização objeto|forte|sem gap crítico aparente|
|média|pessoal|clínica|`app.py:17392`|`app.route('/buscar_animais')`|`login_required`|is_admin, clinic_id check|query|login + autorização objeto|forte|sem gap crítico aparente|
|média|pessoal|usuário|`app.py:17421`|`app.route('/update_tutor/<int:user_id>', methods=['POST'])`|`login_required`|-|URL path, form-data|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:17568`|`app.route('/ficha_tutor/<int:tutor_id>')`|`login_required`|-|URL path|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:17629`|`app.route('/update_animal/<int:animal_id>', methods=['POST'])`|`login_required`|-|URL path, form-data|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:17963`|`app.route('/tipo_racao', methods=['POST'])`|`login_required`|-|json body|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;|
|média|pessoal|usuário|`app.py:18009`|`app.route('/tipo_racao/<int:tipo_id>', methods=['PUT', 'DELETE'])`|`login_required`|-|URL path, json body|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:18051`|`app.route('/buscar_racoes')`|`-`|-|query|login + autorização objeto|parcial|Sem login_required explícito;Sem checagem de autorização/ownership evidente;|
|média|pessoal|usuário|`app.py:18148`|`app.route("/relatorio/racoes")`|`login_required`|-|-|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;|
|média|pessoal|usuário|`app.py:18601`|`app.route("/relatorio/racoes/pesquisa")`|`login_required`|abort(403), is_admin|-|login + autorização objeto|forte|sem gap crítico aparente|
|média|pessoal|usuário|`app.py:18641`|`app.route("/relatorio/racoes/pesquisa/<int:tutor_id>/toggle", methods=["POST"])`|`login_required`|abort(403), is_admin|URL path|login + autorização objeto|forte|ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:18684`|`app.route("/relatorio/racoes/pesquisa/<int:tutor_id>/status/<status_key>", methods=["POST"])`|`login_required`|abort(403), is_admin|URL path|login + autorização objeto|forte|ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:18790`|`app.route("/relatorio/racoes/pesquisa/<int:tutor_id>/answers", methods=["POST"])`|`login_required`|abort(403), is_admin|URL path, form-data|login + autorização objeto|forte|ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:18859`|`app.route("/relatorio/racoes/pesquisa/send-selected", methods=["POST"])`|`login_required`|abort(403), is_admin|form-data|login + autorização objeto|forte|sem gap crítico aparente|
|média|pessoal|usuário|`app.py:18972`|`app.route("/relatorio/racoes/pesquisa/warmup-whatsapp", methods=["POST"])`|`login_required`|abort(403), is_admin|-|login + autorização objeto|forte|sem gap crítico aparente|
|média|pessoal|usuário|`app.py:18996`|`app.route("/historico_animal/<int:animal_id>")`|`login_required`|-|URL path|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:19396`|`app.route("/medicamento", methods=["POST"])`|`login_required`|-|json body|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;|
|média|pessoal|usuário|`app.py:19438`|`app.route("/medicamento/<int:med_id>", methods=["PUT", "DELETE"])`|`login_required`|-|URL path, json body|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:19590`|`app.route("/medicamentos_favoritos", methods=["GET"])`|`login_required`|-|-|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;|
|média|pessoal|usuário|`app.py:19612`|`app.route("/medicamentos_favoritos/<int:med_id>", methods=["POST", "DELETE"])`|`login_required`|-|URL path|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|média|pessoal|clínica|`app.py:20314`|`app.route('/criar_tutor_ajax', methods=['POST'])`|`login_required`|clinic_id check|form-data|login + autorização objeto|forte|sem gap crítico aparente|
|média|pessoal|usuário|`app.py:20632`|`app.route('/animal/<int:animal_id>/marcar_falecido', methods=['POST'])`|`login_required`|-|URL path, form-data|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:20664`|`app.route('/animal/<int:animal_id>/reverter_falecimento', methods=['POST'])`|`login_required`|abort(403)|URL path|login + autorização objeto|forte|ID na URL sem validação de escopo clara;|
|média|pessoal|usuário|`app.py:20687`|`app.route('/animal/<int:animal_id>/arquivar', methods=['POST'])`|`login_required`|-|URL path|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|média|pessoal|clínica|`app.py:26591`|`app.route('/servico', methods=['POST'])`|`login_required`|-|json body|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;|
|baixa|outros|usuário|`app.py:1217`|`app.route("/surpresa")`|`-`|-|-|login + autorização objeto|parcial|Sem login_required explícito;Sem checagem de autorização/ownership evidente;|
|baixa|outros|usuário|`app.py:1224`|`app.route("/surpresa/<path:filename>")`|`-`|-|URL path|login + autorização objeto|parcial|Sem login_required explícito;Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|baixa|outros|usuário|`app.py:3949`|`app.route('/')`|`-`|-|-|login + autorização objeto|parcial|Sem login_required explícito;Sem checagem de autorização/ownership evidente;|
|baixa|outros|usuário|`app.py:7611`|`app.route('/service-worker.js')`|`-`|-|-|login + autorização objeto|parcial|Sem login_required explícito;Sem checagem de autorização/ownership evidente;|
|baixa|outros|usuário|`app.py:17948`|`app.route('/api/tipos_racao', methods=['GET'])`|`login_required`|-|-|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;|
|baixa|outros|usuário|`app.py:18038`|`app.route('/tipos_racao')`|`-`|-|query|login + autorização objeto|parcial|Sem login_required explícito;Sem checagem de autorização/ownership evidente;|
|baixa|outros|usuário|`app.py:19005`|`app.route('/relatorio/racoes/<int:tipo_id>')`|`login_required`|-|URL path|login + autorização objeto|média|Sem checagem de autorização/ownership evidente;ID na URL sem validação de escopo clara;|
|baixa|outros|usuário|`app.py:19367`|`app.route('/importar_medicamentos')`|`-`|-|-|login + autorização objeto|parcial|Sem login_required explícito;Sem checagem de autorização/ownership evidente;|
|baixa|outros|usuário|`app.py:19470`|`app.route("/apresentacao_medicamento", methods=["POST"])`|`-`|-|json body|login + autorização objeto|parcial|Sem login_required explícito;Sem checagem de autorização/ownership evidente;|
|baixa|outros|usuário|`app.py:19557`|`app.route("/buscar_apresentacoes")`|`-`|-|query|login + autorização objeto|parcial|Sem login_required explícito;Sem checagem de autorização/ownership evidente;|
## Governança contínua de segurança de rotas

### Security champions (revisão de rotas sensíveis)
- Cada squad/área deve nomear pelo menos 1 security champion titular e 1 suplente.
- PRs que alterem rotas sensíveis (dados clínicos, financeiros, PII, permissões admin ou integrações externas) exigem revisão de um security champion antes do merge.
- O champion deve validar explicitamente autorização por objeto/tenant e risco de IDOR, com comentário no PR.

### Cadência de vistorias
- **Mensal**: vistoria dos endpoints críticos (alta criticidade na tabela deste documento), com amostragem de fluxos e validação de controles de autorização.
- **Trimestral**: vistoria completa de rotas (alta, média e baixa), incluindo revalidação de exceções registradas e atualização da matriz de risco.
- Os resultados devem ser registrados com data, escopo, responsáveis, gaps e plano de remediação.

### Treinamento rápido do time (OWASP Top 10)
- Realizar um treinamento curto recorrente para o time com foco em OWASP Top 10.
- Dar ênfase em **Broken Access Control** (incluindo IDOR), com exemplos reais das rotas da aplicação e checklist de prevenção para PR.
- Novos integrantes devem concluir o treinamento na integração inicial e reciclar trimestralmente.
