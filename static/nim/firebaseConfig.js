// Arquivo mantido apenas por compatibilidade com a versão antiga baseada em Firebase.
// Agora usamos Socket.IO para sincronizar o estado do jogo, portanto não há configuração aqui.
export const firebaseConfig = {};
